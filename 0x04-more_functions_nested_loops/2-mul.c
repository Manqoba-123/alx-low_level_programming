#include "main.h"

/**
 * mul -> prints product of 2 numbers
 * @a: first integer
 * @b: second interger
 * Return: returns the product
 */

int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
