#include "main.h"

/**
 * _puts -> returns the string
 * @str: string
 * Return: returns a string value
 */

void _puts(char *str)
{
	int i;

	while (str[i)
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
